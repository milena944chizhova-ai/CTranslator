/*
 * interpreter.h — интерфейс интерпретатора
 *
 * Выполняет программу по синтаксическому дереву AST.
 * Все переменные хранятся в статическом массиве (имя → значение).
 * Специальное значение INT_MIN обозначает «не инициализировано».
 */
#ifndef INTERPRETER_H
#define INTERPRETER_H

#include "ast.h"

/*
 * interpret() — запустить программу.
 * Возвращает 0 при успехе, -1 при ошибке времени выполнения.
 */
int interpret(AstNode *root);

#endif /* INTERPRETER_H */
